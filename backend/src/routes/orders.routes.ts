import {Router} from "express";import {requireAuth} from "../middleware/auth.middleware";import {asyncHandler} from "../utils/async-handler";import * as c from "../controllers/order.controller";const r=Router();r.post("/",requireAuth,asyncHandler(c.place));r.put("/:orderId",requireAuth,asyncHandler(c.modify));r.delete("/:orderId",requireAuth,asyncHandler(c.cancel));export default r;
