import {Router} from "express";import {requireAuth} from "../middleware/auth.middleware";import {asyncHandler} from "../utils/async-handler";import * as c from "../controllers/broker.controller";const r=Router();r.get("/session",requireAuth,asyncHandler(c.getSession));r.post("/session",requireAuth,asyncHandler(c.initiate));r.post("/session/refresh",requireAuth,asyncHandler(c.refresh));r.get("/session/callback",asyncHandler(c.callback));r.post("/session/stream",requireAuth,asyncHandler(c.startStream));r.delete("/session/stream",requireAuth,asyncHandler(c.stopStream));export default r;
