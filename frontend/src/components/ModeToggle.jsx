import { useState } from "react";
export default function ModeToggle({ mode="paper", onChange, disabled=false }) { const [confirming,setConfirming]=useState(false); const [confirmation,setConfirmation]=useState(""); const next=mode==="paper"?"live":"paper"; const live=next==="live"; const close=()=>{setConfirming(false);setConfirmation("")}; return <div className="mode-card"><div><span className="eyebrow">Trading mode</span><div className="mode-title"><span className={`mode-dot ${mode}`}/>{mode.toUpperCase()}</div><p className="muted">{mode==="paper"?"Safe simulation mode. Live orders are blocked.":"Live mode unlocked. Keep safeguards enabled."}</p></div><button className={`button ${live?"danger":"secondary"}`} disabled={disabled} onClick={()=>live?setConfirming(true):onChange("paper")}>{live?"Request live mode":"Return to paper"}</button>{confirming&&<div className="confirm-box"><p>Live trading requires the backend gate and exact confirmation string.</p><input placeholder="I_UNDERSTAND_LIVE_TRADING_RISK" value={confirmation} onChange={e=>setConfirmation(e.target.value)} /><button className="button danger" disabled={!confirmation.trim()} onClick={()=>{onChange("live",confirmation.trim());close()}}>Confirm live mode</button><button className="button ghost" onClick={close}>Cancel</button></div>}</div>; }
