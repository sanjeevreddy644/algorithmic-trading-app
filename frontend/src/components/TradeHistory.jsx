const money=n=>Number(n||0).toLocaleString(undefined,{style:"currency",currency:"INR",maximumFractionDigits:2});
export default function TradeHistory({ id, trades=[] }) { return <section className="panel" id={id}><div className="panel-header"><div><span className="eyebrow">Execution</span><h2>Trade history</h2></div></div>{trades.length===0?<div className="empty-state">No trade history is available yet. The dashboard will populate this when the backend exposes the trade-history endpoint.</div>:<div className="table-wrap"><table><thead><tr><th>Time</th><th>Symbol</th><th>Side</th><th>Qty</th><th>Fill</th><th>Status</th><th>P&L</th></tr></thead><tbody>{trades.map(t=><tr key={t._id||t.id}><td>{new Date(t.filledAt||t.submittedAt||Date.now()).toLocaleString()}</td><td><strong>{t.symbol}</strong></td><td><span className={`badge ${t.side}`}>{String(t.side).toUpperCase()}</span></td><td>{t.quantity}</td><td>{money(t.fillPrice||t.requestedPrice)}</td><td><span className={`status ${t.status}`}>{t.status}</span></td><td className={(t.realizedPnl||0)>=0?"positive":"negative"}>{money(t.realizedPnl)}</td></tr>)}</tbody></table></div>}</section>; }
