const money = n => Number(n || 0).toLocaleString(undefined,{style:"currency",currency:"INR",maximumFractionDigits:2});
export default function PositionsTable({ id, positions=[] }) { return <section className="panel" id={id}><div className="panel-header"><div><span className="eyebrow">Portfolio</span><h2>Current positions</h2></div><span className="muted">{positions.length} open</span></div>{positions.length===0?<div className="empty-state">No open positions yet. Paper trades will appear here after a fill.</div>:<div className="table-wrap"><table><thead><tr><th>Symbol</th><th>Qty</th><th>Avg entry</th><th>Mark</th><th>Unrealized P&L</th><th>Realized P&L</th></tr></thead><tbody>{positions.map(p=><tr key={p._id||p.symbol}><td><strong>{p.symbol}</strong></td><td>{p.quantity}</td><td>{money(p.averageEntryPrice)}</td><td>{money(p.markPrice)}</td><td className={p.unrealizedPnl>=0?"positive":"negative"}>{money(p.unrealizedPnl)}</td><td className={p.realizedPnl>=0?"positive":"negative"}>{money(p.realizedPnl)}</td></tr>)}</tbody></table></div>}</section>; }
