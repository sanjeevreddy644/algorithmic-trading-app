import { useState } from "react";
import { useAuth } from "../context/AuthContext";
export default function Login({ onRegister }) { const {signIn}=useAuth(); const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false); return <AuthLayout title="Welcome back" subtitle="Sign in to your trading workspace."><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError("");try{await signIn(email,password)}catch(err){setError(err.message)}finally{setBusy(false)}}}><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<div className="form-error">{error}</div>}<button className="button primary full" disabled={busy}>{busy?"Signing in…":"Sign in"}</button></form><p className="auth-switch">New here? <button onClick={onRegister}>Create an account</button></p></AuthLayout>; }
function AuthLayout({title,subtitle,children}){return <main className="auth-shell"><section className="auth-card"><div className="brand large"><span className="brand-mark">AT</span><span>Algorithmic Trading</span></div><span className="eyebrow">Secure workspace</span><h1>{title}</h1><p>{subtitle}</p>{children}</section></main>}
